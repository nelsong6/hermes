# Tofu
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
*.tfvars.json
.terraform.lock.hcl.bak

# Helm
charts/
*.tgz

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Local secrets
.env
.env.*
!.env.example

# Auth-proxy build output. `go build` in auth-proxy/ writes an
# `auth-proxy` binary; never commit it.
/auth-proxy/auth-proxy
